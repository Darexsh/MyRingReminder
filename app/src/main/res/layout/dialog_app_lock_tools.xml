<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_app_lock_toggle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/settings_app_lock_label"
        android:textAllCaps="false"
        android:textSize="15sp"
        app:cornerRadius="12dp"
        app:backgroundTint="#6200EE"
        android:textColor="#FFFFFF" />

    <TextView
        android:id="@+id/tv_app_lock_toggle_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/app_lock_tools_toggle_hint"
        android:textSize="12sp"
        android:textStyle="italic"
        android:textColor="?attr/colorOnBackground"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="12dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_app_lock_delay"
        style="?attr/materialButtonOutlinedStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/settings_app_lock_timeout_label"
        android:textAllCaps="false"
        android:textSize="15sp"
        app:cornerRadius="12dp"
        app:strokeColor="#6200EE"
        app:strokeWidth="2dp"
        android:textColor="#6200EE" />

    <TextView
        android:id="@+id/tv_app_lock_delay_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/app_lock_tools_delay_hint"
        android:textSize="12sp"
        android:textStyle="italic"
        android:textColor="?attr/colorOnBackground"
        android:layout_marginTop="4dp" />

</LinearLayout>
